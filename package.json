{
  "name": "franz-lola-level-editor",
  "private": true,
  "version": "2.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "catalog:generate": "node scripts/generate-game-catalog.mjs",
    "test": "node --test",
    "test:e2e": "playwright test",
    "test:all": "npm test && npm run test:e2e && npm run build"
  },
  "dependencies": {
    "@franz-lola/pixel-renderer": "github:MatthaeusStumptner/Pacman_clone_renderer#v1.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "vite": "^6.4.3"
  }
}
