{
  "name": "franz-lola-publisher",
  "version": "1.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "db:migrate:local": "wrangler d1 migrations apply LEVEL_DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply LEVEL_DB --remote",
    "deploy": "wrangler deploy --keep-vars",
    "types": "wrangler types",
    "test": "node --test"
  },
  "dependencies": {
    "@franz-lola/pixel-renderer": "github:MatthaeusStumptner/Pacman_clone_renderer#c3d2c91"
  },
  "devDependencies": {
    "wrangler": "^4.118.0"
  },
  "overrides": {
    "undici": "7.29.0"
  },
  "engines": {
    "node": ">=22.3.0"
  }
}
